class Car {
    fun cars(classCars: Cars) {
        println(
            "cars make ${classCars.carMake} \n cars model ${classCars.model} \n " +
                    "cars engine ${classCars.engine} \n cars mass ${classCars.mass} \n" +
                    " cars Color ${classCars.color}"
        )
    }
}